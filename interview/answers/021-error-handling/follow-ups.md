# 021. error 处理 - 面试追问

## 追问与参考答案

### 1. 什么时候应该定义哨兵错误？

当某种错误是 API 契约的一部分，调用方需要稳定识别并做分支处理时，可以定义哨兵错误。不要把内部实现错误随意暴露成哨兵错误，否则后续重构会被错误类型兼容性绑住。

### 2. 为什么不要只返回字符串错误？

只返回字符串错误会丢失可编程判断能力，调用方只能做脆弱的字符串比较。更好的方式是包装原始错误、保留上下文，并通过 `errors.Is` 或 `errors.As` 判断。
