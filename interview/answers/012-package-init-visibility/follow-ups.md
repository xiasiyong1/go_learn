# 012. 包、可见性和 init - 面试追问

## 追问与参考答案

### 1. 空白导入 `_ "pkg"` 的作用是什么？

空白导入用于只执行被导入包的初始化副作用，而不直接引用其中的标识符。典型例子是数据库驱动注册、图片格式注册等。

### 2. 一个包里可以有多个 init 函数吗？

一个包里可以有多个 init 函数，同一文件内按出现顺序执行。跨文件顺序不应该作为业务依赖，真正有顺序要求的初始化应显式组织在普通函数里。
