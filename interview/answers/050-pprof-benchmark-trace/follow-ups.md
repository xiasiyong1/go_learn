# 050. benchmark、pprof 和 trace - 面试追问

## 追问与参考答案

### 1. flat 和 cum 分别表示什么？

`flat` 表示函数自身直接消耗的资源，不包含它调用的子函数。`cum` 表示函数及其所有下游调用累计消耗的资源，适合看调用链整体成本。

### 2. 为什么优化前要先测量？

性能瓶颈经常和直觉不一致。先测量可以确认真正热点，避免优化无关路径，也能用数据验证改动是否真的有效。
