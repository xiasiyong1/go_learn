# 067. 结构体嵌入

## 问题

Go 结构体嵌入、字段提升和方法提升应该怎么理解？

## 先给结论

结构体嵌入是组合语法，不是继承。匿名字段的字段和方法可以被提升，让外层类型使用起来更方便，但外层类型和内层类型仍然是不同类型。

## 深入理解

### 1. 这道题真正考察什么

- 是否能区分嵌入字段和普通命名字段。
- 是否知道字段提升只是选择器语法便利。
- 是否能解释方法提升和接口实现之间的关系。
- 是否知道嵌入可能导致字段或方法名冲突。

### 2. 底层机制要讲清楚

- 匿名字段的类型名会作为字段名，外层可以通过短选择器访问其字段和方法。
- 提升不改变实际内存结构，也不建立子类关系。
- 外层类型的方法集可能包含被提升的方法，从而实现某些接口。
- 当多个嵌入字段提供同名成员时，选择器可能变得歧义，必须显式指定路径。

### 3. 工程实践怎么取舍

- 用嵌入复用小而稳定的行为，例如组合锁、日志器或基础能力。
- 对外 API 谨慎暴露匿名字段，避免把内部实现提升成公共接口。
- 需要明确领域关系时，普通命名字段比匿名嵌入更清晰。
- 接口实现依赖方法提升时，要写编译期断言。

### 4. 常见误区

- 把嵌入理解成继承，误以为外层可以替代内层类型。
- 嵌入导出类型后，无意暴露大量内层方法。
- 字段名冲突导致访问歧义或读者误解。
- 嵌入含锁结构体后复制外层值，触发 copylocks 问题。

## 如何验证理解

- 写小例子验证外层类型不能赋值给内层类型变量。
- 用编译期断言检查方法提升是否让外层实现接口。
- 用 `go vet -copylocks` 检查嵌入锁后的复制风险。

## 面试追问

追问参考答案：[follow-ups.md](follow-ups.md)

- 如果继续追问“结构体嵌入”的底层机制，应该讲到哪些层次？
- 这个知识点在真实项目里应该如何取舍？
- 最容易踩的坑是什么？为什么这些坑不是背结论就能避免的？
- 如何用测试、工具或 profiling 验证自己的判断？
- 当数据量、并发量或团队规模变大后，这个问题会怎样升级？
