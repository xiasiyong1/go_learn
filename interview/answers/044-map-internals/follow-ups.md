# 044. map 底层实现 - 面试追问

## 追问与参考答案

### 1. 为什么 map 取元素返回的是两个值？

取元素返回两个值是为了区分 key 不存在和 value 恰好是零值。`v, ok := m[k]` 中 ok 表示是否真的找到了这个 key。

### 2. map 中的元素为什么不能直接取地址？

map 元素不能直接取地址，因为 map 扩容和渐进迁移时元素位置可能变化，返回稳定地址会破坏运行时实现。需要修改字段时，通常取出值、修改后再写回，或让 map 存指针。
