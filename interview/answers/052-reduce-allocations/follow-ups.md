# 052. 减少内存分配 - 面试追问

## 追问与参考答案

### 1. 为什么过早优化会让代码变复杂？

过早优化容易牺牲可读性和正确性，还可能优化错地方。应该先用 benchmark、pprof 或线上指标定位热点，再只优化对结果有影响的路径。

### 2. `strings.Builder` 和 `bytes.Buffer` 怎么选？

`strings.Builder` 主要用于高效拼接字符串，避免反复创建中间字符串。`bytes.Buffer` 更适合处理字节流、I/O 和需要 `[]byte` 的场景。
