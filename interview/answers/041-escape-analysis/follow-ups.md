# 041. 逃逸分析 - 面试追问

## 追问与参考答案

### 1. 返回局部变量指针一定有问题吗？

没有问题。Go 编译器会通过逃逸分析发现这个局部变量在函数返回后仍被引用，于是把它分配到堆上，保证指针有效。代价是增加堆分配和 GC 压力。

### 2. 接口转换为什么可能导致逃逸？

接口转换可能需要把具体值装箱成接口值，编译器有时无法证明这个值的生命周期只在当前栈帧内，尤其是传给外部函数或存入接口容器时。因此它可能逃逸到堆上。
