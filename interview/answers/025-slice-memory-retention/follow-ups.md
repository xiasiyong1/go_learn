# 025. 切片内存滞留 - 面试追问

## 追问与参考答案

### 1. `copy` 和 `append([]T(nil), src...)` 有什么区别？

`copy` 需要先创建目标切片，然后把元素复制进去；`append([]T(nil), src...)` 是常见的一行写法，本质也是创建新底层数组再复制。两者都能切断和原底层数组的引用。

### 2. 子切片的 cap 为什么重要？

子切片的 cap 决定它还能向后看到多少原底层数组空间，也决定 append 是否可能继续复用原数组。必要时可以用完整切片表达式 `s[a:b:b]` 限制 cap，减少意外覆盖。
