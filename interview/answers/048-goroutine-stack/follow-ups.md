# 048. goroutine 栈 - 面试追问

## 追问与参考答案

### 1. goroutine 数量是否可以无限增长？

不可以。goroutine 虽然轻量，但仍然占用栈、调度器元数据和可能的业务资源。无限创建会导致内存膨胀、调度开销增加，甚至拖垮下游系统。

### 2. 深递归对 goroutine 栈有什么影响？

深递归会不断消耗 goroutine 栈，runtime 可以按需扩栈，但扩栈有成本。递归过深仍可能导致性能问题或达到运行时限制，工程中要谨慎。
