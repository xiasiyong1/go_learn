# 015. 结构体基础

## 问题

结构体值拷贝、匿名字段和 tag 分别怎么理解？

## 先给结论

结构体是字段的聚合值，默认按值拷贝。理解结构体时要同时关注字段可见性、匿名字段、tag、可比较性、内存布局和嵌套引用字段。

## 深入理解

### 1. 这道题真正考察什么

- 是否知道结构体赋值和传参会复制所有字段。
- 是否能解释匿名字段是组合，不是传统继承。
- 是否知道 tag 只是字符串元数据，需要反射或框架解释。
- 是否能判断结构体是否可比较。

### 2. 底层机制要讲清楚

- 结构体值拷贝是浅拷贝：字段本身会复制，但字段指向的数据可能仍共享。
- 匿名字段会提升字段和方法，但不会建立父子类型关系。
- 结构体 tag 编译器不理解其业务含义，JSON、ORM、校验器等库通过反射读取。
- 所有字段都可比较时，结构体才可比较；含 slice、map、func 时不可比较。

### 3. 工程实践怎么取舍

- 小而不可变的数据结构适合值语义。
- 含锁、缓存、连接、上下文等状态的结构体一般不应随意复制。
- tag 用于边界层映射，不要让领域模型被过多框架 tag 污染。
- 组合可以复用行为，但对外暴露匿名字段要注意 API 泄露。

### 4. 常见误区

- 以为结构体拷贝会深拷贝内部 slice 或 map。
- 匿名嵌入导致字段名冲突或方法集变化。
- tag 格式写错，编译能过但运行时行为不符合预期。
- 把含 mutex 的结构体复制后继续使用。

## 如何验证理解

- 写测试验证结构体拷贝后内部 slice/map 是否共享。
- 用 `go vet` 检查 tag 格式和 copylocks。
- 用反射小例子读取 tag，理解框架如何工作。

## 代码示例

```go
type User struct {
	ID   int    `json:"id"`
	Name string `json:"name"`
}
```

## 面试追问

追问参考答案：[follow-ups.md](follow-ups.md)

- 如果继续追问“结构体基础”的底层机制，应该讲到哪些层次？
- 这个知识点在真实项目里应该如何取舍？
- 最容易踩的坑是什么？为什么这些坑不是背结论就能避免的？
- 如何用测试、工具或 profiling 验证自己的判断？
- 当数据量、并发量或团队规模变大后，这个问题会怎样升级？
