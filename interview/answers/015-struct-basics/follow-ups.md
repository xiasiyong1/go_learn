# 015. 结构体基础 - 面试追问

## 追问与参考答案

### 1. 结构体能不能比较？

结构体只有在所有字段都可比较时才可以比较。包含 slice、map、function 字段的结构体不能直接用 `==` 比较。

### 2. 组合和继承有什么区别？

组合是把一个类型嵌入另一个类型，获得字段和方法提升，但它不是子类型关系。继承强调父子层级和多态复用，Go 更鼓励通过组合和小接口表达能力。
