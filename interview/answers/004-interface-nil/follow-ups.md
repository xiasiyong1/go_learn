# 004. interface nil 陷阱 - 面试追问

## 追问与参考答案

### 1. `interface{}` 的内部结构是什么？

接口值可以理解为类型信息和值两部分。空接口通常是动态类型和值，非空接口通常还包含方法表信息；只有类型和值都为空时，接口才等于 nil。

### 2. 类型断言和类型判断分别适合什么场景？

类型断言适合你预期接口里就是某个具体类型的场景，推荐使用 `v, ok := x.(T)`。type switch 适合一个接口可能有多种具体类型，需要按类型分支处理的场景。
