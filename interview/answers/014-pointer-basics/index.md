# 014. 指针基础

## 问题

Go 指针能做什么？为什么不能做指针运算？

## 先给结论

Go 指针用于间接访问和共享修改，但不支持指针运算。面试要能说明指针带来的可变性、逃逸、nil 风险和 API 语义，而不是简单说“传指针更快”。

## 深入理解

### 1. 这道题真正考察什么

- 是否知道 `&` 取地址、`*` 解引用，以及 nil 指针解引用会 panic。
- 是否能解释 Go 禁止普通指针运算是为了内存安全和 GC 正确性。
- 是否能根据对象大小、是否需要修改、是否含锁等因素选择传值或传指针。
- 是否知道返回局部变量指针在 Go 中是安全的，编译器会做逃逸分析。

### 2. 底层机制要讲清楚

- 指针保存变量地址，多个指针可以指向同一个对象，因此会引入共享可变状态。
- GC 需要准确知道对象引用关系，任意指针运算会破坏安全边界。
- 传指针减少值拷贝，但可能让对象逃逸到堆上，增加 GC 成本。
- 值传递复制的是值本身；如果值里有 slice、map、指针字段，内部数据仍可能共享。

### 3. 工程实践怎么取舍

- 需要修改调用方对象、对象较大、方法要保持一致性时用指针。
- 小的不可变值、时间值、坐标值等可以传值，语义更清晰。
- 包含 `sync.Mutex` 等不可复制字段的结构体，不要在使用后复制。
- 对外 API 不要为了“可能更快”到处暴露指针，先表达清楚所有权和可变性。

### 4. 常见误区

- 以为传指针一定更快，忽略逃逸和缓存局部性。
- 值拷贝含锁结构体，导致锁保护失效。
- 把 nil 指针作为正常值传递，却没有在方法里定义行为。
- 误以为不能返回局部变量地址。

## 如何验证理解

- 用 benchmark 比较小值、大值、指针传递的实际表现。
- 用 `-gcflags=-m` 查看是否逃逸到堆。
- 用 `go vet -copylocks` 检查复制锁的问题。

## 代码示例

```go
func addOne(n *int) {
	(*n)++
}
```

## 面试追问

追问参考答案：[follow-ups.md](follow-ups.md)

- 如果继续追问“指针基础”的底层机制，应该讲到哪些层次？
- 这个知识点在真实项目里应该如何取舍？
- 最容易踩的坑是什么？为什么这些坑不是背结论就能避免的？
- 如何用测试、工具或 profiling 验证自己的判断？
- 当数据量、并发量或团队规模变大后，这个问题会怎样升级？
