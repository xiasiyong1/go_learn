# 014. 指针基础

## 问题

Go 指针能做什么？为什么不能做指针运算？

## 核心答案

Go 指针保存变量地址，可以避免大对象拷贝，也可以让函数修改调用方传入的值。

Go 不支持普通指针运算，这是为了降低内存错误和越界访问风险。需要底层地址操作时只能使用 `unsafe`，但这会绕过类型安全。

## 代码示例

```go
func addOne(n *int) {
	(*n)++
}
```

## 面试追问

追问参考答案：[follow-ups.md](follow-ups.md)

- 什么时候传指针，什么时候传值？
- 指针是否一定比值传递更快？
