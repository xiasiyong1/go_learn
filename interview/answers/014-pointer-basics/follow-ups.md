# 014. 指针基础 - 面试追问

## 追问与参考答案

### 1. 什么时候传指针，什么时候传值？

需要修改调用方对象、避免复制大对象、对象里有锁或其他不能复制的字段时，通常传指针。小的、不可变语义的数据传值更清楚，也可能更利于编译器优化。

### 2. 指针是否一定比值传递更快？

不一定。指针可以减少复制，但也可能导致变量逃逸到堆上，增加 GC 成本，并带来缓存局部性变差的问题。是否更快要结合对象大小、生命周期和 benchmark 判断。
