# 046. interface 底层实现 - 面试追问

## 追问与参考答案

### 1. 接口调用为什么可能有动态派发成本？

接口调用需要根据动态类型找到对应方法实现，可能无法像普通静态调用那样直接内联或静态绑定，所以有动态派发成本。不过在大多数业务代码中，这个成本通常不是主要瓶颈。

### 2. 接口转换为什么可能分配内存？

接口转换可能把具体值装箱到接口中，如果编译器无法证明接口值不会逃逸，就需要把数据放到堆上。大对象或非指针值装入接口时尤其要关注分配。
