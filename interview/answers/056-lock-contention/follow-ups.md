# 056. 锁竞争 - 面试追问

## 追问与参考答案

### 1. 分片锁的代价是什么？

分片锁能降低单个锁的竞争，但会增加实现复杂度，并让跨分片操作更难维护一致性。分片数量、哈希分布和热点 key 都会影响效果。

### 2. RWMutex 读多写少一定更好吗？

不一定。RWMutex 有更复杂的状态管理，写锁还会和读锁互相影响。只有读多写少且读临界区足够重时，它才可能比 Mutex 更合适。
