# 018. 可比较类型 - 面试追问

## 追问与参考答案

### 1. interface 比较时什么时候会 panic？

接口值比较时，如果两个接口的动态类型相同但该动态类型不可比较，会发生 panic。例如接口里装的是 slice，再执行 `a == b` 就会 panic。

### 2. 为什么 map 的 key 必须可比较？

map 需要通过 key 的哈希值定位 bucket，并用相等比较确认具体 key。不可比较类型没有稳定的相等语义，因此不能作为 map key。
