# 018. 可比较类型

## 问题

Go 中哪些类型可以比较？`comparable` 表示什么？

## 先给结论

可比较类型可以使用 `==` 和 `!=`，也可以作为 map key。`comparable` 是泛型约束，表示类型集合中的类型都支持比较，但接口值比较还有运行时 panic 边界。

## 深入理解

### 1. 这道题真正考察什么

- 是否知道 slice、map、func 不可比较，只能和 nil 比较。
- 是否能判断数组和结构体是否可比较取决于元素或字段。
- 是否知道接口比较时，动态类型不可比较会 panic。
- 是否理解泛型中的 `comparable` 约束服务于编译期类型集合。

### 2. 底层机制要讲清楚

- 基础类型、指针、channel、可比较元素组成的数组和结构体都可比较。
- slice、map、func 内部包含引用状态或函数值，语言不定义通用相等语义。
- map key 需要稳定的哈希和相等判断，所以必须可比较。
- 接口静态类型看似可比较，但比较时要看其中装入的动态值是否可比较。

### 3. 工程实践怎么取舍

- 需要把复合数据作为 key 时，优先设计可比较结构体或编码成稳定字符串。
- 需要比较 slice 内容时，用 `slices.Equal` 或自定义比较，而不是 `==`。
- 泛型函数需要使用 `==` 时，加 `comparable` 约束。
- 对接口值做比较前，确认不会装入 slice、map、func 等不可比较动态值。

### 4. 常见误区

- 结构体新增 slice 字段后，原本可比较的类型突然不可比较。
- 把 `any` 值直接放进 map key 或做 `==`，运行时 panic。
- 把指针相等误认为内容相等。
- 泛型里忘记加 comparable，却在函数体中使用 `==`。

## 如何验证理解

- 写编译期例子验证哪些类型可比较。
- 用单测覆盖接口中装入不可比较类型后比较 panic 的情况。
- 对 map key 类型新增字段时跑编译和测试，确认可比较性没有被破坏。

## 代码示例

```go
func Index[T comparable](items []T, target T) int {
	for i, item := range items {
		if item == target {
			return i
		}
	}
	return -1
}
```

## 面试追问

追问参考答案：[follow-ups.md](follow-ups.md)

- 如果继续追问“可比较类型”的底层机制，应该讲到哪些层次？
- 这个知识点在真实项目里应该如何取舍？
- 最容易踩的坑是什么？为什么这些坑不是背结论就能避免的？
- 如何用测试、工具或 profiling 验证自己的判断？
- 当数据量、并发量或团队规模变大后，这个问题会怎样升级？
