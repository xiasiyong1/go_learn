# 001. 数组和切片 - 面试追问

## 追问与参考答案

### 1. 为什么大切片截取小切片后，可能导致内存无法释放？

小切片仍然持有原底层数组的引用，只要小切片还活着，GC 就会认为整个底层数组仍然可达。只保留少量数据时，应该把需要的部分复制到新的切片里，让大数组可以被回收。

### 2. 如果要避免共享底层数组，应该怎么复制切片？

可以使用 `dst := make([]T, len(src)); copy(dst, src)`，也可以使用 `dst := append([]T(nil), src...)`。核心目标是让新切片拥有独立底层数组，不再共享原数组。
