# 024. 泛型 - 面试追问

## 追问与参考答案

### 1. 泛型约束是什么？

泛型约束描述类型参数必须满足的能力，例如是否可比较、是否支持排序、是否属于某组底层类型。约束本身通常是接口类型。

### 2. 泛型和 interface 分别适合表达什么抽象？

泛型适合表达同一算法适用于多个具体类型，并且需要保留静态类型信息的场景。interface 适合表达行为能力和运行时多态，例如只关心某个值是否有 `Read` 方法。
