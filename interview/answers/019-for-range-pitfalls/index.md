# 019. for range 常见坑

## 问题

`for range` 有哪些常见坑？

## 先给结论

`for range` 简洁但隐藏了复制语义、循环变量复用、map 顺序随机和遍历期间修改等细节。面试重点是能否根据遍历对象判断真实行为。

## 深入理解

### 1. 这道题真正考察什么

- 是否知道 range 数组会复制数组，range 切片复制切片头。
- 是否知道循环变量每轮复用，取地址或闭包捕获要小心。
- 是否知道 range map 顺序不稳定。
- 是否能判断遍历中修改 slice、map 的边界。

### 2. 底层机制要讲清楚

- range 得到的是元素副本，直接修改循环变量不会修改原元素。
- 遍历切片时，range 开始会确定遍历长度，append 导致的变化不一定被遍历到。
- map 遍历顺序被故意打乱，避免程序依赖实现细节。
- Go 1.22 改善了部分循环变量闭包问题，但理解变量作用域仍然重要，老代码和不同写法仍需谨慎。

### 3. 工程实践怎么取舍

- 需要修改切片元素时，用索引 `for i := range s { s[i] = ... }`。
- 需要稳定顺序时，先取 key、排序，再按 key 访问 map。
- 遍历 map 时删除当前或未访问 key 通常安全，但新增元素是否遍历到不应依赖。
- 并发遍历和修改 map 必须加锁或换并发模型。

### 4. 常见误区

- 对 range 变量取地址，得到多个相同地址或非预期值。
- 修改结构体元素副本，原 slice 没变。
- 在遍历切片时 append，同一个底层数组被修改导致结果难推理。
- 写测试依赖 map 遍历顺序，偶发失败。

## 如何验证理解

- 用小程序打印 range 变量地址和值，理解副本和复用。
- 写测试覆盖修改结构体切片元素的正确写法。
- 对输出 map 的函数做排序，避免测试依赖随机顺序。

## 代码示例

```go
users := []User{{Name: "a"}}
for _, u := range users {
	u.Name = "b" // 不会修改 users[0]
}
```

## 面试追问

追问参考答案：[follow-ups.md](follow-ups.md)

- 如果继续追问“for range 常见坑”的底层机制，应该讲到哪些层次？
- 这个知识点在真实项目里应该如何取舍？
- 最容易踩的坑是什么？为什么这些坑不是背结论就能避免的？
- 如何用测试、工具或 profiling 验证自己的判断？
- 当数据量、并发量或团队规模变大后，这个问题会怎样升级？
