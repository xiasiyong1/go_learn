# 019. for range 常见坑 - 面试追问

## 追问与参考答案

### 1. 如何在 range 中正确修改切片元素？

应该通过索引修改原元素，例如 `for i := range users { users[i].Name = "x" }`。如果切片里存的是指针，也可以修改指针指向的对象，但要确认指针共享符合预期。

### 2. 遍历 map 时删除元素是否安全？

遍历 map 时删除当前或其他 key 是安全的。遍历过程中新增 key 不保证是否会被本轮遍历到，所以不能依赖新增元素的遍历行为。
