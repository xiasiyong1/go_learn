# 009. nil 切片和空切片 - 面试追问

## 追问与参考答案

### 1. JSON 中 nil slice 和 empty slice 序列化结果有什么区别？

使用 `encoding/json` 时，nil slice 通常序列化为 `null`，空 slice 通常序列化为 `[]`。这会影响 API 客户端对字段含义的判断。

### 2. API 返回列表时应该返回 nil 还是空切片？

列表接口一般更推荐返回空切片，表示“有这个列表，但当前没有元素”，客户端也更容易处理。只有当 `null` 本身有明确业务含义时，才应该返回 nil。
