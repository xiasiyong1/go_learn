# 068. 接口

## 问题

Go 的接口为什么是隐式实现？接口应该由谁来定义？

## 先给结论

Go 类型只要拥有接口要求的方法集，就自动实现接口，不需要显式声明。接口通常应由使用方定义，因为使用方最清楚自己需要什么行为。

## 深入理解

### 1. 这道题真正考察什么

- 是否知道 Go 没有 `implements` 关键字。
- 是否能解释方法集决定是否实现接口。
- 是否理解小接口比大接口更灵活。
- 是否知道接口通常放在消费者侧，而不是生产者侧。

### 2. 底层机制要讲清楚

- 编译器根据类型方法集和接口方法集判断实现关系。
- 值类型和指针类型的方法集不同，影响接口实现。
- 隐式实现让类型可以在不依赖接口包的情况下满足接口。
- 接口变量保存动态类型和值，运行时调用会进行动态派发。

### 3. 工程实践怎么取舍

- 只为真实使用场景抽象接口，不提前为每个结构体定义接口。
- 接口方法越少越好，常见如 `io.Reader` 只有一个核心方法。
- 使用编译期断言记录关键实现关系。
- mock 需求出现时，优先在调用方定义最小接口。

### 4. 常见误区

- 在生产者包里为每个 struct 配一个大接口。
- 接口方法过多，导致实现困难和测试笨重。
- 忽略指针接收者方法导致只有 `*T` 实现接口。
- 把接口当成泛型容器，到处使用 `interface{}`。

## 如何验证理解

- 用 `var _ Interface = (*T)(nil)` 做编译期断言。
- 写测试用假实现替换真实依赖，检查接口是否足够小。
- 用 `go vet` 和编译错误发现方法集不匹配。

## 面试追问

追问参考答案：[follow-ups.md](follow-ups.md)

- 如果继续追问“接口”的底层机制，应该讲到哪些层次？
- 这个知识点在真实项目里应该如何取舍？
- 最容易踩的坑是什么？为什么这些坑不是背结论就能避免的？
- 如何用测试、工具或 profiling 验证自己的判断？
- 当数据量、并发量或团队规模变大后，这个问题会怎样升级？
