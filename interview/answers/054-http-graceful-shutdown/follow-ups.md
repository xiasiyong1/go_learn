# 054. HTTP 优雅关闭 - 面试追问

## 追问与参考答案

### 1. `Shutdown` 和 `Close` 有什么区别？

`Shutdown` 会停止接收新连接，并等待已有请求在 context 截止前完成。`Close` 会立即关闭监听器和连接，更粗暴，可能中断正在处理的请求。

### 2. 长连接和后台任务如何处理？

长连接需要协议层设计退出方式，例如设置读写 deadline、发送关闭信号或限制最大连接时长。后台任务要接收 context 或独立的停止信号，并在服务关闭流程中等待它们退出。
