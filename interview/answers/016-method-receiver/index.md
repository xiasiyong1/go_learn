# 016. 值接收者和指针接收者

## 问题

值接收者和指针接收者应该怎么选？

## 先给结论

值接收者和指针接收者的选择不是语法偏好，而是在表达修改能力、复制成本、方法集和接口实现。一个类型的方法接收者最好保持一致。

## 深入理解

### 1. 这道题真正考察什么

- 是否知道值接收者会复制接收者，指针接收者共享原对象。
- 是否能解释指针接收者方法和接口实现的关系。
- 是否知道可寻址值可以自动取地址调用指针接收者方法。
- 是否能根据类型大小、可变性、是否含锁选择接收者。

### 2. 底层机制要讲清楚

- 方法接收者本质上是函数的第一个参数。
- T 的方法集只包含值接收者方法，*T 的方法集包含值接收者和指针接收者方法。
- 接口实现检查方法集，因此只有 *T 可能实现了某个含指针接收者方法的接口。
- 自动取地址只是调用语法糖，不改变方法集规则。

### 3. 工程实践怎么取舍

- 需要修改接收者或避免复制大对象时用指针接收者。
- 小的不可变值类型可以用值接收者，例如类似 time.Time 的语义。
- 含 mutex、buffer、cache 等内部状态的类型应使用指针接收者。
- 同一类型不要混用接收者，除非有非常清楚的理由。

### 4. 常见误区

- 值接收者方法里修改字段，以为会影响原对象。
- 把 T 传给接口，结果只有 *T 实现接口。
- 方法值绑定接收者时复制了值，后续原对象变化不影响已绑定方法值。
- 为性能盲目改指针接收者，导致共享可变状态扩大。

## 如何验证理解

- 写编译期断言 `var _ Interface = (*T)(nil)` 验证接口实现。
- 写测试区分值接收者修改副本和指针接收者修改原对象。
- 用 benchmark 评估大结构体值接收者复制成本。

## 代码示例

```go
type Counter struct {
	n int
}

func (c *Counter) Inc() {
	c.n++
}
```

## 面试追问

追问参考答案：[follow-ups.md](follow-ups.md)

- 如果继续追问“值接收者和指针接收者”的底层机制，应该讲到哪些层次？
- 这个知识点在真实项目里应该如何取舍？
- 最容易踩的坑是什么？为什么这些坑不是背结论就能避免的？
- 如何用测试、工具或 profiling 验证自己的判断？
- 当数据量、并发量或团队规模变大后，这个问题会怎样升级？
