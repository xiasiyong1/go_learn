# 016. 值接收者和指针接收者 - 面试追问

## 追问与参考答案

### 1. 值类型变量能调用指针接收者方法吗？

如果变量是可寻址的，编译器可以自动取地址，所以值变量也能调用指针接收者方法。不可寻址的临时值不能调用需要指针接收者的方法。

### 2. 指针接收者和接口实现有什么关系？

方法集决定一个类型是否实现接口。类型 `T` 的方法集只包含值接收者方法，`*T` 的方法集包含值接收者和指针接收者方法；如果接口需要指针接收者方法，通常只有 `*T` 实现它。
