# 066. map 操作

## 问题

Go map 的查找、删除、零值返回和 `comma ok` 应该怎么理解？

## 先给结论

map 查找不存在的 key 会返回值类型的零值，因此需要 `comma ok` 区分“不存在”和“存在但值正好是零值”。`delete` 对不存在的 key 是安全的，但 nil map 只能读和删，不能写。

## 深入理解

### 1. 这道题真正考察什么

- 是否知道 `m[k]` 不存在时返回元素类型零值。
- 是否能解释 `v, ok := m[k]` 的意义。
- 是否知道 `delete(m, k)` 对不存在 key 不报错。
- 是否知道 nil map 读、删安全，写会 panic。

### 2. 底层机制要讲清楚

- map 查找返回一个值副本；单返回值形式无法表达 key 是否存在。
- `comma ok` 通过第二个布尔值告诉调用方 key 是否存在。
- `delete` 是内置函数，不要求 key 一定存在。
- nil map 没有底层哈希表，写入时无法放置元素，因此 panic。

### 3. 工程实践怎么取舍

- 计数场景可以利用零值：`m[k]++`。
- 缓存或配置读取需要区分零值时必须使用 comma ok。
- 删除前不需要先判断存在，直接 delete 即可。
- 创建需要写入的 map 时用 `make`，并根据规模给容量提示。

### 4. 常见误区

- 把空字符串、0、false 当成 key 不存在。
- 对 nil map 赋值导致 panic。
- 直接修改 map 中结构体字段，忘记 map 返回的是副本。
- 并发读写普通 map，导致数据竞争或运行时 fatal error。

## 如何验证理解

- 写测试覆盖不存在 key 和存在但值为零值的场景。
- 写 nil map 读、删、写的小例子理解边界。
- 用 `go test -race` 检查并发访问 map 的代码。

## 面试追问

追问参考答案：[follow-ups.md](follow-ups.md)

- 如果继续追问“map 操作”的底层机制，应该讲到哪些层次？
- 这个知识点在真实项目里应该如何取舍？
- 最容易踩的坑是什么？为什么这些坑不是背结论就能避免的？
- 如何用测试、工具或 profiling 验证自己的判断？
- 当数据量、并发量或团队规模变大后，这个问题会怎样升级？
