# 049. 三色标记和写屏障 - 面试追问

## 追问与参考答案

### 1. 写屏障为什么只在 GC 特定阶段开启？

写屏障有运行时成本，所以只在 GC 标记等需要维护并发标记正确性的阶段开启。非 GC 阶段没有必要为每次指针写入付出额外成本。

### 2. 为什么低延迟 GC 不等于没有成本？

低延迟 GC 只是把长停顿拆散并发执行，不代表没有代价。GC 仍会消耗 CPU、内存带宽，并可能影响分配速度和整体吞吐。
