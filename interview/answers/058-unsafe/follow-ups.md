# 058. unsafe - 面试追问

## 追问与参考答案

### 1. `unsafe.Pointer` 和 `uintptr` 有什么区别？

`unsafe.Pointer` 是 Go 指针和任意指针类型之间转换的桥梁，GC 仍可能把它视为指针。`uintptr` 只是整数，GC 不会把它当成对象引用，不能长期保存对象地址。

### 2. 为什么 unsafe 代码要特别注意对象生命周期？

unsafe 代码如果把地址转成整数或构造非标准引用，可能让 GC 看不到真实引用，导致对象被提前回收或移动假设被破坏。必须确保对象在使用期间仍然被正常 Go 引用保持存活。
