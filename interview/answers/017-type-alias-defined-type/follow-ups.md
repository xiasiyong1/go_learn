# 017. 类型别名和新类型 - 面试追问

## 追问与参考答案

### 1. 为什么给基础类型定义新类型有价值？

给基础类型定义新类型可以表达领域含义，避免把不同概念的相同底层类型混用，例如 `UserID` 和 `OrderID`。新类型还可以绑定方法，让行为和数据语义聚合在一起。

### 2. 类型别名适合在哪些重构场景使用？

类型别名适合包迁移、API 重命名和渐进式重构。它能让旧路径和新路径在类型系统里仍然是同一个类型，降低破坏性变更。
