# 043. Go 内存模型 - 面试追问

## 追问与参考答案

### 1. 没有数据竞争是否等于逻辑一定正确？

不是。没有数据竞争只说明内存访问同步上是安全的，但业务执行顺序仍可能不符合预期。例如两个请求按不同顺序更新状态，即使用锁保护，也可能产生业务竞态。

### 2. channel 如何建立内存可见性？

channel 发送 happens-before 对应接收，所以接收方能看到发送前的写入。关闭 channel happens-before 接收方观察到关闭，这也是用关闭 channel 广播退出信号的基础。
