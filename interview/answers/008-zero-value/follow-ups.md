# 008. 零值 - 面试追问

## 追问与参考答案

### 1. nil map 可以读吗？可以写吗？

nil map 可以读取，读取不存在的 key 会返回元素类型零值和 `ok=false`。nil map 不能写入，写入会 panic，因为底层哈希表没有初始化。

### 2. 为什么零值可用能减少构造函数的必要性？

零值可用让类型在声明后就处于可操作状态，减少样板构造代码，也降低忘记初始化的风险。不过涉及外部资源、配置校验或复杂不变式时，构造函数仍然有价值。
