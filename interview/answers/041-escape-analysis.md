# 041. 逃逸分析

## 问题

什么是逃逸分析？如何判断变量分配在栈上还是堆上？

## 核心答案

逃逸分析是编译器判断变量生命周期和可见范围的过程。如果变量可能在函数返回后仍被使用，或者编译器无法证明它只在当前栈帧内使用，就可能逃逸到堆上。

是否逃逸不是由 `new` 或取地址单独决定，而是由使用方式决定。

## 代码示例

```bash
go build -gcflags="-m" ./...
```

## 面试追问

- 返回局部变量指针一定有问题吗？
- 接口转换为什么可能导致逃逸？
