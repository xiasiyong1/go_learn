# 013. 函数、方法、闭包和可变参数 - 面试追问

## 追问与参考答案

### 1. 闭包捕获循环变量会有什么问题？

旧版本 Go 中 range 循环变量会被复用，闭包可能捕获到同一个变量，导致 goroutine 中看到的值不符合预期。Go 1.22 调整了常见 range 场景的语义，但阅读旧代码或兼容旧版本时仍应显式传参或局部拷贝。

### 2. 方法值和方法表达式有什么区别？

方法值会把接收者绑定进去，例如 `f := v.M`，之后调用 `f()`。方法表达式不会绑定接收者，例如 `f := T.M`，调用时需要显式传入接收者。
