# 011. const 和 iota - 面试追问

## 追问与参考答案

### 1. iota 遇到空行或显式赋值时如何变化？

`iota` 在同一个 const 声明块中按常量声明行递增，空行不影响它。显式赋值会使用当前 iota，后续省略表达式的常量会复用上一行表达式但 iota 继续递增。

### 2. Go 为什么没有传统 enum？

Go 没有传统 enum 是为了保持语言和类型系统简单。通常用自定义类型加 const 表达枚举语义，如果需要严格校验，再补充 `String`、`Valid` 或解析函数。
