# 039. race detector - 面试追问

## 追问与参考答案

### 1. 数据竞争和竞态条件有什么区别？

数据竞争是内存访问层面的未同步并发读写，race detector 能检测。竞态条件是业务执行顺序导致结果不符合预期，即使没有数据竞争，也可能有竞态条件。

### 2. race 检测为什么会让程序变慢？

race 检测会插桩记录内存访问和同步事件，运行时需要维护额外元数据并做检查，所以 CPU 和内存开销都会明显增加。它适合测试和预发排查，不适合生产常态开启。
