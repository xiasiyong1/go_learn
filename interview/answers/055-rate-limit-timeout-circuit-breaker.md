# 055. 限流、超时和熔断

## 问题

如何设计限流、超时和熔断？

## 核心答案

限流用于控制入口或下游调用并发，避免资源被打满。常见算法有令牌桶、漏桶和固定窗口。

超时用于给请求设置最大等待时间，防止调用链无限挂起。

熔断用于在下游持续失败时快速失败，给下游恢复时间，同时保护当前服务。

## 代码示例

```go
ctx, cancel := context.WithTimeout(parent, 200*time.Millisecond)
defer cancel()
```

## 面试追问

- 限流和熔断解决的问题有什么区别？
- 超时时间应该如何设置？
