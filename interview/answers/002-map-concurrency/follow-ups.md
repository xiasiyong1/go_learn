# 002. map 并发安全 - 面试追问

## 追问与参考答案

### 1. `sync.Map` 和 `map` 加锁的适用场景有什么区别？

`sync.Map` 适合读多写少、key 相对稳定、每个 key 独立读写的缓存场景。普通 `map` 加 `sync.RWMutex` 更适合需要类型安全、复合操作、维护多个字段一致性的业务逻辑。

### 2. 为什么只读并发访问通常是安全的，但一旦有写入就不安全？

只读并发访问不会修改 map 内部结构，通常没有数据竞争。一旦有写入，map 可能发生 bucket 修改、扩容和迁移，并发读可能看到不一致状态，所以不安全。
