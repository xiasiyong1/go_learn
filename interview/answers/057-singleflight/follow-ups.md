# 057. singleflight - 面试追问

## 追问与参考答案

### 1. singleflight 和互斥锁有什么区别？

互斥锁只是保护临界区，通常每个等待者拿到锁后仍会执行自己的逻辑。singleflight 会让同一个 key 的并发调用共享同一次执行结果，重点是合并重复工作。

### 2. 如果执行函数很慢或失败会怎样？

如果执行函数很慢，其他同 key 请求会一起等待，所以仍然需要超时和取消控制。如果执行失败，等待者通常会共享同一个错误，调用方要决定是否重试或降级。
