# 035. atomic 和 Mutex

## 问题

原子操作和互斥锁如何选择？

## 核心答案

原子操作适合简单、独立的数值或指针读写，例如计数器、状态标记。它开销低，但表达复杂不变式很困难。

互斥锁适合保护多个字段之间的一致性，代码更容易读懂，也更不容易写错。

默认选择锁，只有在性能路径明确且逻辑简单时再考虑 atomic。

## 代码示例

```go
var n atomic.Int64
n.Add(1)
fmt.Println(n.Load())
```

## 面试追问

- atomic 能否替代所有锁？
- 原子操作和内存可见性有什么关系？
