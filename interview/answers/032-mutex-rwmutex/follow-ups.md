# 032. Mutex 和 RWMutex - 面试追问

## 追问与参考答案

### 1. `defer Unlock` 有什么优缺点？

`defer Unlock` 能保证异常路径也释放锁，代码更安全清晰。缺点是释放时机固定在函数返回，热点小临界区里可能有一点 defer 成本，也可能让锁持有时间比必要更长。

### 2. RWMutex 一定比 Mutex 快吗？

不一定。RWMutex 只有在读多写少、读临界区较重且竞争明显时才可能有优势；如果写多、临界区很短或读写切换频繁，RWMutex 可能更慢。
