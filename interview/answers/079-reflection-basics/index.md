# 079. 反射

## 问题

Go 反射能做什么？为什么说反射要谨慎使用？

## 先给结论

反射让程序在运行时检查和操作类型和值，常用于 JSON、ORM、配置解析和通用工具。它牺牲编译期类型安全和性能，使用时要明确边界和错误处理。

## 深入理解

### 1. 这道题真正考察什么

- 是否知道 `reflect.Type` 和 `reflect.Value` 的区别。
- 是否能解释可设置值和不可设置值。
- 是否知道反射访问未导出字段有限制。
- 是否能说明反射为什么比直接代码更容易出错。

### 2. 底层机制要讲清楚

- `reflect.TypeOf` 获取动态类型信息，`reflect.ValueOf` 获取运行时值表示。
- 只有可寻址且可设置的 Value 才能通过反射修改。
- 反射调用方法和字段访问会进行运行时检查，错误用 panic 表现的场景不少。
- 反射通常伴随接口装箱、动态分派和额外分配。

### 3. 工程实践怎么取舍

- 框架层、序列化层和工具层可以使用反射。
- 业务核心逻辑优先使用静态类型、接口或泛型。
- 反射代码要把 panic 风险转成明确错误。
- 反射结果可缓存时缓存 Type 元数据，降低重复解析成本。

### 4. 常见误区

- 对不可设置 Value 调用 Set，运行时 panic。
- 没有处理指针和值的差异，导致字段访问失败。
- 通过反射绕开类型系统，让错误延迟到运行时。
- 在高频路径大量使用反射，造成 CPU 和分配热点。

## 如何验证理解

- 写测试覆盖指针、值、nil、未导出字段和类型不匹配。
- 用 benchmark 比较反射实现和静态实现。
- 用 pprof 确认反射是否是性能热点。

## 面试追问

追问参考答案：[follow-ups.md](follow-ups.md)

- 如果继续追问“反射”的底层机制，应该讲到哪些层次？
- 这个知识点在真实项目里应该如何取舍？
- 最容易踩的坑是什么？为什么这些坑不是背结论就能避免的？
- 如何用测试、工具或 profiling 验证自己的判断？
- 当数据量、并发量或团队规模变大后，这个问题会怎样升级？
