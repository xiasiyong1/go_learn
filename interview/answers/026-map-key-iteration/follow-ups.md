# 026. map key 和遍历顺序 - 面试追问

## 追问与参考答案

### 1. 如果需要稳定顺序应该怎么做？

需要稳定顺序时，先把 key 收集到切片中排序，然后按排序后的 key 访问 map。不要依赖 map 的 range 顺序。

### 2. 结构体可以作为 map key 吗？

可以，但前提是结构体的所有字段都可比较。包含 slice、map、function 字段的结构体不能作为 map key。
