# 034. sync.Once - 面试追问

## 追问与参考答案

### 1. sync.Once 和 init 函数有什么区别？

`init` 在包初始化时自动执行，无法按需控制，也不方便返回错误。`sync.Once` 是运行期懒初始化，可以在真正需要资源时执行，并保证并发安全。

### 2. 初始化失败应该如何处理？

如果初始化可能失败，不要简单把错误吞掉。常见做法是把结果和错误都存在外部变量中，`once.Do` 内赋值，调用方拿到同一个错误；如果需要失败后重试，`sync.Once` 本身就不适合，需要额外状态机。
