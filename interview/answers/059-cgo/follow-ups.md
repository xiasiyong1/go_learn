# 059. cgo - 面试追问

## 追问与参考答案

### 1. Go 指针传给 C 有哪些限制？

Go 指针传给 C 受到限制，核心原则是不能让 C 长期保存可能指向 Go 堆对象的指针，也不能让 C 持有包含 Go 指针的内存结构。否则 GC 无法正确追踪对象关系。

### 2. 为什么大量小 cgo 调用会影响性能？

每次 cgo 调用都要跨越 Go runtime 和 C ABI 边界，涉及栈、调度和线程状态切换。大量细粒度调用会把边界开销放大，通常应批量化或改用纯 Go 实现。
