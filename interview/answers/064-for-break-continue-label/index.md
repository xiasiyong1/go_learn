# 064. 循环

## 问题

Go 为什么只有 `for` 循环？`break`、`continue` 和标签怎么用？

## 先给结论

Go 用一个 `for` 覆盖传统 for、while 和无限循环。`break`、`continue` 默认作用于最近一层循环或 switch，标签可以控制外层循环，但滥用会降低可读性。

## 深入理解

### 1. 这道题真正考察什么

- 是否知道 `for condition {}` 相当于 while。
- 是否知道 `for {}` 是无限循环。
- 是否能解释 break 在 switch 和循环嵌套里的作用对象。
- 是否知道标签可以用于跳出或继续外层循环。

### 2. 底层机制要讲清楚

- Go 没有 while 关键字，减少控制流语法种类。
- `break` 不带标签时退出最近的 for、switch 或 select。
- `continue` 不带标签时进入最近 for 的下一轮。
- 带标签的 break/continue 让多层循环可以直接控制外层结构。

### 3. 工程实践怎么取舍

- 普通遍历用 `for i := 0; i < n; i++` 或 `for range`。
- 条件循环用 `for condition`，比无限循环加内部 break 更直接。
- 多层循环需要提前退出时可以用标签，但要保持标签名表达意图。
- 复杂嵌套通常应拆函数，通过 return 简化控制流。

### 4. 常见误区

- 在 switch 中 break，只退出 switch，不退出外层 for。
- 无限循环缺少退出条件，导致 CPU 空转或 goroutine 泄漏。
- 标签滥用让控制流像 goto 一样难读。
- range 中修改集合时没有理解遍历语义。

## 如何验证理解

- 写嵌套 for + switch 小例子验证 break 的目标。
- 对循环退出条件写边界测试，覆盖空输入和异常输入。
- 用 pprof 或测试超时发现意外无限循环。

## 面试追问

追问参考答案：[follow-ups.md](follow-ups.md)

- 如果继续追问“循环”的底层机制，应该讲到哪些层次？
- 这个知识点在真实项目里应该如何取舍？
- 最容易踩的坑是什么？为什么这些坑不是背结论就能避免的？
- 如何用测试、工具或 profiling 验证自己的判断？
- 当数据量、并发量或团队规模变大后，这个问题会怎样升级？
