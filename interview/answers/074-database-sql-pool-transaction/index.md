# 074. database/sql

## 问题

`database/sql` 的连接池、事务和 Rows 关闭应该怎么理解？

## 先给结论

`*sql.DB` 不是单个连接，而是并发安全的连接池句柄。正确使用 database/sql 的关键是复用 DB、配置连接池、关闭 Rows、控制事务生命周期和区分查询错误时机。

## 深入理解

### 1. 这道题真正考察什么

- 是否知道 `sql.DB` 应长期复用而不是每次打开。
- 是否知道 `Rows` 必须关闭。
- 是否能解释 QueryRow 的错误在 Scan 时返回。
- 是否能正确处理事务 commit 和 rollback。

### 2. 底层机制要讲清楚

- `sql.DB` 管理空闲和活跃连接，内部会按需建立连接。
- `Rows` 持有连接资源，不关闭会影响连接池可用连接数。
- 事务 `Tx` 绑定到单个连接，必须 Commit 或 Rollback 结束。
- 连接池参数包括最大打开连接、最大空闲连接、连接生命周期和空闲时间。

### 3. 工程实践怎么取舍

- 应用启动时创建 DB，退出时关闭。
- 每次 Query 后 `defer rows.Close()`，并在遍历后检查 `rows.Err()`。
- 事务中使用 `defer tx.Rollback()` 兜底，成功后 Commit。
- 根据数据库容量和服务并发配置连接池，不要无限打开连接。

### 4. 常见误区

- 每个请求 Open 一次 DB，导致连接风暴。
- Rows 未关闭，连接池耗尽。
- 事务失败路径忘记 Rollback，连接长期占用。
- 连接池上限过大，把数据库打满。

## 如何验证理解

- 用集成测试覆盖查询、空结果、事务失败和提交失败。
- 监控 `DB.Stats()` 中 open、in-use、idle、wait count。
- 压测不同连接池配置，观察数据库延迟和等待时间。

## 面试追问

追问参考答案：[follow-ups.md](follow-ups.md)

- 如果继续追问“database/sql”的底层机制，应该讲到哪些层次？
- 这个知识点在真实项目里应该如何取舍？
- 最容易踩的坑是什么？为什么这些坑不是背结论就能避免的？
- 如何用测试、工具或 profiling 验证自己的判断？
- 当数据量、并发量或团队规模变大后，这个问题会怎样升级？
