# 003. defer、return 和具名返回值 - 面试追问

## 追问与参考答案

### 1. `defer` 的参数什么时候求值？

`defer` 的参数会在执行到 defer 语句时立即求值。闭包 defer 的函数体是在返回前执行，所以闭包内部读取到的是执行时变量的值。

### 2. 在循环中使用 `defer` 有什么风险？

循环中使用 `defer` 会把清理动作推迟到整个函数返回，可能导致文件句柄、锁或连接长时间不释放。通常用内部函数包住每轮逻辑，或者在循环中显式释放资源。
