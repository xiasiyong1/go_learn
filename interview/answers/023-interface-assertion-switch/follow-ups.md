# 023. 类型断言和 type switch - 面试追问

## 追问与参考答案

### 1. `any` 和 `interface{}` 有区别吗？

`any` 是 `interface{}` 的类型别名，两者完全等价。`any` 只是让代码在表达任意类型时更短、更直观。

### 2. 接口设计为什么应该尽量小？

小接口更容易被实现、测试和复用，也能减少调用方和实现方的耦合。接口越大，实现者负担越重，也越容易变成不清晰的抽象。
