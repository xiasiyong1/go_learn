# 038. 测试 - 面试追问

## 追问与参考答案

### 1. `t.Helper()` 有什么作用？

`t.Helper()` 标记当前函数是测试辅助函数。失败时测试框架会把报错位置指向调用辅助函数的测试代码，而不是辅助函数内部，更利于定位问题。

### 2. 单元测试和集成测试如何划分？

单元测试关注单个函数或组件的逻辑，依赖尽量可控且速度快。集成测试覆盖多个组件、外部服务或真实存储的协作，成本更高但能发现边界和配置问题。
