# 020. JSON 和结构体 tag - 面试追问

## 追问与参考答案

### 1. 如何区分字段没传和字段传了零值？

常见做法是把字段定义为指针，例如 `*int`，nil 表示没传，非 nil 且值为 0 表示传了零值。更复杂场景可以自定义 Unmarshal，额外记录字段是否出现过。

### 2. 自定义 JSON 序列化应该实现哪个接口？

自定义序列化实现 `json.Marshaler`，自定义反序列化实现 `json.Unmarshaler`。这样可以控制字段格式、兼容旧协议或处理特殊时间格式。
