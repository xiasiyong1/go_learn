# 011. const 和 iota

## 问题

`const` 和 `iota` 怎么用？有哪些常见坑？

## 核心答案

`const` 定义编译期常量，只能是布尔、数字、字符串等可在编译期确定的值。

`iota` 在一个 const 声明块中从 0 开始，每新增一行常量声明递增 1，常用于枚举和位标志。

## 代码示例

```go
const (
	Read = 1 << iota
	Write
	Execute
)
```

上面得到的值分别是 1、2、4。

## 面试追问

- iota 遇到空行或显式赋值时如何变化？
- Go 为什么没有传统 enum？
